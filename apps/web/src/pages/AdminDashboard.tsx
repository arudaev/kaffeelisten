// Admin dashboard: month-to-date summary, transaction log, report trigger
// Protected — requires valid session token from AdminLogin PIN flow

export default function AdminDashboard() {
  return (
    <div className="min-h-screen bg-stone-50 p-8">
      <h1 className="text-2xl font-semibold text-stone-900 mb-6">Monatsübersicht</h1>
      <p className="text-stone-600">Dashboard wird aufgebaut.</p>
    </div>
  )
}
